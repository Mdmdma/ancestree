# AWS S3 Configuration for Image Storage
# Copy this file to .env and fill in your actual values

# AWS Access Keys
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# AWS Region where your S3 bucket is located
AWS_REGION=us-east-1

# S3 Bucket name for storing family photos
S3_BUCKET_NAME=ancestree-images

# Note: Make sure to:
# 1. Create an S3 bucket with the name specified above
# 2. Configure proper IAM permissions for the access keys
# 3. Set up CORS policy on the S3 bucket to allow web access
# 4. Never commit the actual .env file to version control
